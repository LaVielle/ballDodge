

function setup(){
	canvas = createCanvas(700, 500);
	canvas.parent('canvas-holder');
	background(0);
}

function draw() {
	background(0);
	for (var  i = 0; i < 10; i ++){
		//ellipse(random(width), random(height), 5, 5)
	}
}